<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.poverty.mapper.CollectionMapper">

    <insert id="insertCollection" parameterType="com.poverty.entity.dto.CollectionDTO">
        insert into t_collection (user_id,article_id,video_id)
        values (#{userId},#{articleId},#{videoId})
    </insert>

    <delete id="deleteCollection" parameterType="com.poverty.entity.dto.CollectionDTO">
        delete user_id,article_id,video_id from t_collection
        where user_id=#{userId} and acticle_id=#{articleId} and video_id=#{videoId}
    </delete>
    <select id="selectCollection" resultType="java.lang.Boolean"
            parameterType="com.poverty.entity.po.Collection">
        select user_id,article_id,video_id,create_time,modify_time from t_collection where user_id=#{userId}
    </select>

</mapper>
